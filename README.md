# ⚙️ Inforadar-Pro-Full

**Inforadar-Pro** — это комплексная платформа для мониторинга и анализа спортивных коэффициентов в реальном времени.  
Система построена на **Docker-инфраструктуре** и объединяет сбор, хранение, визуализацию и алертинг данных о движении коэффициентов.

---

## 🧩 Архитектура проекта

| Сервис                       | Назначение                                      | Порт           |
| ----------------------------- | ----------------------------------------------- | -------------- |
| **MySQL**                    | Хранилище данных (матчи, коэффициенты, история) | `3306`         |
| **Prometheus**               | Сбор метрик с сервисов и экспортёров            | `9090`         |
| **Alertmanager**             | Оповещения (в т.ч. в Telegram)                  | `9093`         |
| **Grafana**                  | Визуализация данных и дашборды                  | `3000`         |
| **Loki + Promtail**          | Централизованный сбор логов                     | `3100`, `9080` |
| **cAdvisor / Node Exporter** | Мониторинг системных метрик контейнеров         | `8085`, `9100` |
| **Inforadar UI (Flask)**     | Веб-интерфейс проекта                           | `5000`         |

---

## 📦 Системные требования

- **OS:** Windows 10/11 (с поддержкой WSL2)  
- **Docker Desktop:** последняя стабильная версия  
- **ОЗУ:** 4 ГБ минимум  
- **Порты:** 3000, 5000, 8085, 9090, 9093, 3100, 9100 (должны быть свободны)

---

## 📂 Структура репозитория

Inforadar-Pro-Full/
├── alertmanager/
│ └── config.yml
├── prometheus/
│ ├── prometheus.yml
│ ├── alert.rules.yml
├── grafana/
│ ├── dashboards/
│ └── provisioning/
│ ├── dashboards/
│ │ └── dashboards.yml
│ └── datasources/
│ └── datasource.yml
├── loki/
│ └── config.yml
├── inforadar_ui/
├── dashboards_backup/
├── docker-compose.yml
├── docker-compose-stable.yml
└── restart_inforadar.ps1


---

## 🚀 Установка и запуск

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/genry1996/Inforadar-Pro-Full.git
cd Inforadar-Pro-Full


# Oddly Odds Production

Платформа для мониторинга и анализа коэффициентов букмекерских контор. Выявляет резкие скачки коэффициентов и другие аномалии, сравнивает линии разных БК и отправляет уведомления в Telegram о потенциально выгодных или подозрительных ставках. 

**Основные возможности (MVP):**
- **Мониторинг коэффициентов**: отслеживание изменений в режиме реального времени по выбранным матчам и рынкам.
- **Генерация алертов**: уведомления о заметных событиях (например, внезапном падении коэффициента) в Telegram-канал или напрямую пользователю.
- **Конфигурация через `config.yaml`**: легко задавать токен бота, идентификатор чата и другие параметры.
- **Гибкая архитектура**: модульное разделение на `alerts/`, `analyzer/` и другие для простого расширения.

**Технологии:** Python 3.10+, aiogram 3.x, YAML для конфигурации, Docker (будет добавлен). 

**Запуск (разработка):**
1. Клонируйте репозиторий: `git clone https://github.com/genry1996/Inforadar-Pro-Full`
2. Установите зависимости: `pip install -r requirements.txt`
3. Создайте и настройте файл `config.yaml`, указав `telegram.token` и `telegram.chat_id`.
4. Запустите бота: `python bot/main.py`

*(Добавьте больше инструкций по развертыванию и использованию в дальнейшем.)* 



🎲 Oddly Odds Production

Oddly Odds Production — это система мониторинга спортивных коэффициентов, предназначенная для обнаружения аномалий в линии ставок. Проект позволяет отслеживать резкие изменения коэффициентов и другие нетипичные события у букмекеров (как в прематч-линии, так и в лайве), сравнивать данные между разными букмекерскими конторами и оперативно уведомлять о найденных аномалиях через Telegram. Таким образом, Oddly Odds помогает игрокам и аналитикам быстро узнавать о необычных движениях линии, подозрительных ситуациях (например, снятие матча с линии или порезка лимитов) и других индикаторах, которые могут указывать на важные события или инсайдерскую информацию.

🧩 Архитектура проекта

Проект построен по модульной архитектуре и развёрнут с помощью Docker. Ниже представлена схема основных компонентов системы и их взаимодействия:


Рис. 1: Схема архитектуры Oddly Odds Production.

Основные компоненты и сервисы:

Сервис	Назначение	Порт
MySQL	Хранилище данных (матчи, коэффициенты, история изменений, аномалии)	3306
Prometheus	Сбор и хранение метрик работы сервисов проекта	9090
OddlyOdds Service (Python + aiogram)	Сбор коэффициентов с API букмекерских контор, детекция аномалий и отправка уведомлений в Telegram	Экспорт метрик на :8000
<!-- (Примечание: сервис OddlyOdds не имеет внешнего порта для пользователя; порт 8000 используется для выдачи метрик Prometheus.) -->

Все компоненты развёрнуты в виде контейнеров Docker и объединены в единую сеть. Python-сервис периодически опрашивает источники данных (API или страницы букмекеров) и сохраняет полученные коэффициенты в базу данных. После каждого обновления данных запускается проверка на аномалии: сравниваются новые коэффициенты с предыдущими значениями и бизнес-правилами. Обнаруженные аномалии фиксируются в базе (в таблице anomalies) и сразу же отправляются в Telegram-канал через бот (реализованный на библиотеке aiogram).

Для мониторинга производительности и стабильности системы используется Prometheus: Python-приложение экспонирует метрики (например, количество обработанных матчей, найденных аномалий, время последнего обновления и т.д.), которые собираются Prometheus’ом. Это позволяет отслеживать состояние системы, строить графики и при необходимости настраивать алерты. Данные хранятся в MySQL, что обеспечивает надежное хранение истории коэффициентов и аномалий для последующего анализа.

📊 Типы отслеживаемых аномалий

Oddly Odds умеет автоматически обнаруживать 9 типов аномалий в движении коэффициентов и поведении линии ставок. Ниже перечислены все типы аномалий, которые трекает система, с кратким описанием каждого:

Резкое падение коэффициента – значительное снижение значения коэффициента за короткий промежуток времени. Например, коэффициент на победу команды мог упасть с 2.50 до 1.80 в течение нескольких минут. Такое падение обычно свидетельствует о том, что на исход делают крупные ставки или появилась инсайдерская информация, из-за чего букмекеры понижают выплату на этот исход, и считается аномальным движением линии.

Резкий рост коэффициента – обратная ситуация: коэффициент резко увеличился (например, с 1.60 до 2.20) за короткий срок. Это может происходить, если резко снизился объем ставок на данный исход или появились новости, уменьшающие вероятность события. Существенный рост коэффициента также рассматривается как аномалия, поскольку обычно коэффициенты не растут быстро без причины.

Порезка лимита – резкое уменьшение максимальной суммы ставки, которую принимает букмекер на данный матч или исход. Если у события лимит ставки, скажем, был 1000 долларов, а стал 10 долларов, это явный сигнал: букмекер ограничивает прием ставок из-за подозрительной активности или стремится снизить возможные убытки. Система отслеживает подобные порезки лимитов как аномалии, поскольку это один из признаков, что с матчем “что-то не так”.

Снятие матча с линии – букмекер полностью удалил событие из линии ставок. Например, матч был доступен для ставок, а затем внезапно исчез из списка. Снятие матча чаще всего происходит из-за подозрений в договорном характере встречи, ошибок в линии или внештатных ситуаций (например, матч отменили). Такой инцидент фиксируется как аномалия наиболее высокого уровня, так как полностью убранный матч – редкое и показательное событие.

Приостановка приёма ставок (заморозка линии) – временная блокировка возможности ставить на матч. В лайве букмекеры часто замораживают линию при ключевых моментах (пенальти, гол, удаление и т.п.), однако вне контекста игровых событий длительная или частая приостановка приема ставок рассматривается как аномалия. Система отслеживает, если матч “завис” в приостановленном состоянии дольше обычного или в прематче ставки внезапно перестали приниматься.

Возобновление приёма ставок – возвращение матча в линию после заморозки или снятия. Само по себе возобновление — положительное событие, однако оно фиксируется, поскольку зачастую идёт в связке с другими аномалиями. Например, если матч был снят, а затем возвращён с сильно изменёнными коэффициентами, это сочетание признаков будет зафиксировано системой (снятие + возобновление с изменением линии). Такой случай тоже даёт инсайты: возможно, букмекер пересмотрел оценку вероятностей после проверки информации.

Существенное изменение линии форы/тотала – резкое смещение предлагаемых букмекером гандикапов или тоталов. Например, тотал голов в матче был 2.5, а стал 3.5, или фора с (-1.0) изменилась на (-2.0). Значительное изменение основных параметров линии говорит о пересмотре ожиданий по исходу матча и фиксируется как аномалия. Обычно такие сдвиги происходят при крупных ставках на определенные исходы или при внезапных новостях (например, неучастие ключевого игрока).

Расхождение коэффициентов между букмекерами – аномалия, возникающая при сравнении нескольких букмекерских контор: коэффициент на один и тот же исход существенно отличается у разных БК. Например, на победу команды А один букмекер даёт 1.70, а другой – 2.20. Такое значительное расхождение может сигнализировать о том, что один из букмекеров не успел обновить линию или имеет иную информацию. Сервис Oddly Odds периодически сверяет коэффициенты разных букмекеров и отмечает случаи, когда разница превышает заданный порог (например, 10-15%).

Арбитражная ситуация (вилка) – частный случай расхождения, когда на противоположных исходах у двух разных букмекеров коэффициенты таковы, что можно гарантированно получить прибыль, поставив на все исходы (т.н. «вилка»). Например, если БК1 даёт коэффициент 2.10 на победу команды X, а БК2 – 2.10 на победу команды Y в том же матче, то, распределив ставки, игрок получит безрисковую прибыль. Такие ситуации крайне редки и быстро устраняются букмекерами, поэтому их обнаружение в режиме реального времени – ценная функция. Oddly Odds отслеживает возникновение вилок между букмекерами как важный вид аномалии, сигнализируя о них немедленно.

Каждой аномалии присваивается тип (код) и сохраняется в базе данных с подробностями: что произошло, старое и новое значение (если применимо), время обнаружения и т.д. Это позволяет не только мгновенно уведомлять пользователей, но и в дальнейшем анализировать накопленные данные (например, какие матчи чаще всего сопровождались аномальными движениями коэффициентов).

📝 Примеры аномалий

Ниже приведены несколько примеров реальных ситуаций, которые бы классифицировались системой как аномалии:

Резкое падение коэффициента (прематч): За сутки до матча Team A vs Team B коэффициент на победу Team A упал с 2.40 до 1.85. Система зафиксировала это как аномалию типа «резкое падение коэффициента». Позже оказалось, что у Team B выбыл из состава ключевой игрок, и информированные бетторы начали массово ставить на Team A, что и вызвало снижение коэффициента. Благодаря Oddly Odds пользователи получили уведомление о движении линии задолго до официальных новостей о травме игрока.

Порезка лимита и снятие матча: В LIVE-матче Player X – Player Y (теннис) букмекер заметил подозрительную активность: на проигрыш фаворита (Player X) начали ставить крупные суммы при счёте, который этого не предполагал. Сначала максимальная ставка на исход «Победа Player Y» была резко снижена (лимит порезан с $500 до $50), а через несколько минут букмекер вовсе убрал матч из линии. Oddly Odds последовательно отправил в канал два оповещения: сначала о порезке лимита, затем о снятии матча. Впоследствии выяснилось, что фаворит действительно получил травму и матч был остановлен – сервис позволил подписчикам заметить неладное ещё в момент, когда информация не была общедоступной.

Вилка между букмекерами: Накануне матча Team C vs Team D коэффициенты разных букмекерских контор разошлись до арбитражного уровня. Букмекер А предлагал 2.05 на победу Team C, в то время как Букмекер B – 2.10 на победу Team D. Одновременная ставка на оба исхода давала бы ~5% гарантированной прибыли, что является явной вилкой. Система Oddly Odds сравнила линию этих БК, обнаружила арбитраж и отправила уведомление типа «вилка» с деталями (коэффициенты 2.05/2.10, потенциальная доходность ~5%). Это позволило заинтересованным подписчикам оперативно воспользоваться ситуацией, пока коэффициенты не выровнялись.

Помимо текстовых уведомлений, имеется возможность просматривать информацию об аномалиях в интерфейсе. Например, на изображении ниже показан фрагмент списка зафиксированных аномалий в веб-интерфейсе Oddly Odds:


Рис. 2: Интерфейс веб-приложения OddlyOdds – список зафиксированных аномалий.

На скриншоте видно, что пользователь может просматривать зарегистрированные аномалии: для каждой указаны тип (иконкой и текстом), букмекер, событие, описание (например, старый и новый коэффициент) и время обнаружения. Фильтры позволяют, к примеру, отобрать аномалии по конкретной букмекерской конторе или типу. Такой интерфейс дополняет Telegram-уведомления, позволяя в удобной форме анализировать историю аномалий.

📂 Структура базы данных

Для хранения информации система использует реляционную базу данных MySQL. Основные таблицы базы данных:

matches – справочник матчей (событий). Содержит информацию о каждом матче: уникальный ID, команды/участники, лига/турнир, дата и время начала, статус (прематч, идет, завершен) и т.п. Другие таблицы ссылаются на matches через этот ID, что позволяет нормализовать данные.

bookmakers – справочник букмекерских контор. Хранит список поддерживаемых БК: ID и название (например, 1 – «Bookmaker A», 2 – «Bookmaker B» и т.д.). Используется для указания, от какого источника получены коэффициенты или аномалия.

odds_history – история коэффициентов. Главная объемная таблица, куда записываются все полученные значения коэффициентов с привязкой ко времени. Каждая запись отражает состояние линии в определенный момент и содержит поля: ID матча (match_id), ID букмекера (bookmaker_id), тип рынка/исхода (например, исход матча 1X2 или тотал), значение коэффициента (odd_value), время фиксации (timestamp), а также признак прематч/лайв. Новая запись добавляется каждый раз, когда система обнаруживает изменение коэффициента у букмекера. Таким образом накапливается исторический ряд коэффициентов для каждого матча.

anomalies – таблица аномалий. В эту таблицу сохраняются все случаи, когда выявлена аномалия. Основные поля: уникальный ID аномалии, ID матча (match_id), ID букмекера (bookmaker_id), тип аномалии (anomaly_type – код или название, соответствующие 9 типам, описанным выше), время обнаружения (detected_at), и детали (например, старое и новое значение коэффициента – для аномалий изменений коэффициентов; старый и новый лимит – для порезки лимита; указание, какой букмекер снял матч – для снятия и т.д.). Таблица anomalies позволяет хранить историю всех аномальных ситуаций. Она связана с таблицами matches и bookmakers по внешним ключам, что облегчает получение человекочитаемых описаний (подставляя названия команд, турниров, БК и т.п. в сообщения).

(Дополнительно) subscriptions – (опциональная таблица) если предусмотрена индивидуальная подписка пользователей на определенные виды аномалий или матчи, может существовать таблица подписок с параметрами для каждого пользователя/канала. Однако в базовой модели Oddly Odds рассылка идёт в единый Telegram-канал, поэтому сложной модели пользователей не требуется – достаточно настроить сам канал и бота.

Все таблицы объединены связями: например, каждая запись в anomalies указывает, к какому матчу и какой БК она относится, используя IDs. Это позволяет гибко делать выборки, например, «все аномалии по матчам лиги X за последний месяц» или «все случаи порезки лимитов у букмекера Y». Схема базы данных спроектирована для хранения большого количества записей истории коэффициентов – таблица odds_history может содержать десятки тысяч записей ежедневно, поэтому особое внимание уделено индексам (по match_id, timestamp и другим полям) для быстрого поиска данных.

📂 Структура проекта

Исходный код и конфигурация организованы по папкам следующим образом:

OddlyOddsProduction/
├── app/                       # Основной код приложения
│   ├── collector.py           # Модуль сбора данных (коэффициенты прематч/лайв)
│   ├── detector.py            # Модуль логики обнаружения аномалий
│   ├── notifier.py            # Модуль отправки уведомлений (Telegram бот)
│   ├── config.py              # Конфигурация приложения (параметры, ключи API и т.д.)
│   └── __init__.py
├── data/                      # Дополнительные данные (например, списки матчей, вспомогательные файлы)
├── docker-compose.yml         # Конфигурация Docker: описание сервисов (БД, приложение, Prometheus)
├── Dockerfile                 # Образ для Python-приложения OddlyOdds
├── prometheus/
│   └── prometheus.yml         # Настройки Prometheus (targets для сбора метрик, интервалы и пр.)
├── .env                       # Файл с чувствительными данными (токен бота Telegram, доступ к БД и др.)
└── README.md                  # Документация проекта (настоящее описание)


Примечание: В папке app/ файлы условно показаны упрощенно. В реальности код может быть разбит на большее количество модулей и пакетов (например, отдельные поддиректории для работы с разными букмекерскими API, утилиты, модели данных и т.д.). Файл config.py/.env содержит такие параметры, как API-ключи, токен Telegram бота, настройки порогов аномалий (например, на сколько процентов должен измениться коэффициент, чтобы считать изменение аномальным), и другие настройки, которые легко менять без правки исходного кода.

Docker-compose файл описывает как минимум три сервиса: базу данных (MySQL), сервис OddlyOdds (Python приложение) и сервис Prometheus. При запуске Docker Compose автоматически поднимает контейнер с MySQL (инициализируя базу и таблицы), контейнер с нашим приложением (которое сразу начинает сбор данных) и Prometheus для метрик. Все пароли и ключи задаются через переменные окружения, подключаемые из .env.

🚀 Развёртывание через Docker

Развернуть Oddly Odds Production можно быстро с помощью Docker. Предполагается, что Docker и Docker Compose уже установлены в системе. Ниже приведены основные шаги установки:

Клонирование репозитория:
Скачайте исходный код проекта с GitHub (или другого источника):

git clone https://github.com/yourname/OddlyOddsProduction.git
cd OddlyOddsProduction


Конфигурация окружения:
Перед первым запуском необходимо задать некоторые параметры. Переименуйте файл с шаблоном переменных окружения:

cp .env.example .env


Откройте файл .env в любом редакторе и укажите реальные значения переменных:

TELEGRAM_BOT_TOKEN: токен вашего Telegram-бота, через которого будут отправляться уведомления (его можно получить у BotFather).

MYSQL_ROOT_PASSWORD: пароль для администратора MySQL (и другие настройки базы, если нужно).

При необходимости скорректируйте прочие параметры (например, пороги для аномалий, интервал опроса API букмекеров и т.д., если они выносятся в .env).

Запуск контейнеров:
Выполните команду Docker Compose для сборки образов и запуска всех сервисов:

docker-compose up -d


Эта команда в фоновом режиме запустит все необходимые контейнеры. Приложение OddlyOdds автоматически подключится к указанным букмекерским конторам, начнёт собирать коэффициенты и отслеживать аномалии. MySQL БД будет запущена и готова принимать данные, а Prometheus начнёт собирать метрики.

Проверка работы:
Убедитесь, что все сервисы запустились корректно:

Командой docker-compose ps проверьте, что состояния контейнеров oddlyodds_app, mysql и prometheus — Up.

Если какой-то контейнер остановился сразу после старта, посмотрите логи (docker-compose logs <имя_сервиса>) для диагностики.

Спустя пару минут после запуска, вы должны видеть первые сообщения об аномалиях в Telegram (если в отслеживаемых матчах что-то происходит). Также вы можете зайти в интерфейс Prometheus (по умолчанию http://localhost:9090) и убедиться, что метрики от нашего приложения доступны (например, метрика с количеством аномалий).

При необходимости остановить работу, выполните docker-compose down (данные при этом сохранятся в volume MySQL). Обновление проекта до новой версии происходит через git pull и перезапуск контейнеров.

🔔 Подписка на уведомления

Oddly Odds уведомляет о найденных аномалиях через специально созданный Telegram-канал. Чтобы начать получать уведомления, необходимо подписаться на этот канал. Алгоритм следующий:

Найдите канал в Telegram: канал называется, например, «OddlyOdds Alerts» (точное название/ссылка будут указаны в документации или на сайте проекта). Перейдите по пригласительной ссылке или найдите его через поиск в приложении Telegram.

Присоединитесь к каналу: нажмите «Join»/«Вступить», после чего вы станете подписчиком и будете получать все публикуемые там сообщения. Уведомления представляют собой краткие сообщения с информацией об аномалии: указываются матч, тип аномалии, букмекер и ключевые детали (например, “коэффициент упал с X до Y за N минут” или “БК Z снял матч из линии”).

Настройте уведомления (опционально): убедитесь, что у вас включены уведомления для данного Telegram-канала (иконка колокольчика не зачёркнута), чтобы оперативно получать сообщения. При желании можно отключить звук уведомлений, чтобы сообщения приходили тихо.

Если проект предусматривает кастомизацию подписки (например, подписка только на определенные виды аномалий или на конкретные турниры), эти настройки выполняются через команды бота в личном чате. В базовом же варианте канал транслирует все аномалии, и подписавшись на него, вы получаете полную ленту событий. Это удобно, так как вы не пропустите ничего важного: вся информация приходит в одном месте и в хронологическом порядке.

💻 Используемые технологии

Проект Oddly Odds Production построен с использованием следующих технологий и инструментов:

Python 3 – основной язык, на котором написана логика приложения. Используются возможности асинхронного программирования для одновременной работы с несколькими источниками данных (например, async HTTP запросы к API букмекеров).

Aiogram – фреймворк для разработки Telegram-ботов на Python. Отвечает за интеграцию с Telegram: отправку уведомлений в канал, обработку команд (если реализовано управление подпиской через бота), и другие взаимодействия.

Docker – контейнеризация всего проекта. Каждый компонент (приложение, БД, системы мониторинга) работает в своем контейнере, что облегчает деплоймент и поддержку. Docker обеспечивает единообразие окружения и возможность легко масштабировать или обновлять отдельные части.

Docker Compose – оркестрация многоконтейнерного приложения. С помощью docker-compose.yml описаны все сервисы, их образы, переменные окружения, тома для данных (например, volume для хранения базы MySQL), а также сетевые порты. Compose позволяет одной командой запускать или останавливать весь стек.

MySQL – реляционная база данных, хранящая информацию о матчах, коэффициентах и аномалиях. Выбрана за надежность и удобство использования SQL для сложных запросов (например, аналитика по истории коэффициентов).

Prometheus – система мониторинга и алертинга. В контексте Oddly Odds используется для сбора метрик приложения (как технических – нагрузка, время ответа, так и бизнес-метрик – количество обнаруженных аномалий, частота обновления коэффициентов и т.д.). Метрики экспортируются приложением в формате, который опрашивает Prometheus. При необходимости Prometheus можно настроить на дополнительные оповещения (например, уведомлять разработчиков, если долго нет новых данных от какого-то букмекера).

Grafana (планируется) – хотя на данный момент Grafana не указана в основном стекe, в будущем возможно её использование для визуализации данных. Связка Prometheus + Grafana позволит строить наглядные дашборды: графики движения коэффициентов, статистику по аномалиям (сколько каких типов за период) и т.д. Сейчас акцент сделан на Telegram-уведомлениях, но технологически данные открыты для подключения инструментов визуализации.

Другие библиотеки: Requests / Aiohttp (для запросов к API), SQLAlchemy или PyMySQL (для взаимодействия с MySQL), и др. – стандартные библиотеки, упрощающие разработку. В Docker-образ включены все необходимые зависимости, так что окружение готово “из коробки”.

🏅 План развития

Проект Oddly Odds находится в активном развитии. В ближайших планах и направлениях улучшения:

Подключение новых букмекеров: Расширение охвата за счёт интеграции с большим количеством букмекерских контор и источников данных. Это повысит шансы обнаруживать аномалии, так как будут анализироваться линии разных БК (в том числе и зарубежных). В приоритете — добавление API крупнейших мировых букмекеров и бирж ставок, а также локальных контор для региональных событий.

Сравнение между БК в режиме реального времени: Далее развитие функционала межбукмекерского сравнения коэффициентов. Сейчас отслеживаются вилки и большие расхождения, но планируется более тонкий анализ: например, вычисление справедливого коэффициента по рынку и определение, какие букмекеры сильно выбиваются из этого среднего значения. Это позволит находить value ставки – случаи, когда коэффициент у какого-то букмекера завышен относительно общего рынка, что выгодно для игрока.

Расчёт value ставок: В связке с предыдущим пунктом будет внедрен расчет показателя value для каждого обнаруженного события. Value-ставка — это ставка с положительным ожидаемым выигрышем, когда вероятность исхода (по мнению аналитической модели или среднего рынка) выше, чем та, что подразумевается коэффициентом букмекера. Oddly Odds сможет подсвечивать такие ситуации, рекомендовать обратить на них внимание. Для этого планируется использовать агрегированные данные по коэффициентам и, возможно, машинное обучение для оценки вероятностей исходов.

Улучшение алгоритмов детекции: Будет проводиться дальнейшая настройка чувствительности и точности правил обнаружения аномалий. Например, применение статистических методов и ML-алгоритмов для выявления нетипичных паттернов, кластеризация матчей по схожести аномалий (чтобы, например, выявлять группы матчей, подозрительно ведущих себя одинаково). Кластеризация данных об аномалиях поможет обнаруживать скрытые взаимосвязи (например, синдикаты ставок, влияющих одновременно на несколько матчей).

Масштабируемость и отказоустойчивость: По мере роста числа источников и объёма данных планируется оптимизировать производительность. Возможный переход на более масштабируемые решения: распределенная обработка (например, разнести сбор данных и анализ по разным сервисам), шардирование базы данных или использование движка БД, рассчитанного на большие нагрузки. Также рассматривается интеграция механизмов резервирования — например, дублирование бота на случай, если основной выйдет из строя, и бэкап данных в облачное хранилище.

Интерактивность и настройки пользователя: В дальнейшем, чтобы не перегружать пользователей лишними уведомлениями, может быть добавлен настраиваемый фильтр подписки. Пользователь (через бот) сможет указать, какие типы аномалий ему интересны, по каким лигам или командам присылать уведомления, в какие часы и т.д. Это повысит ценность сервиса для разных категорий пользователей – от арбитражников до тех, кто охотится за валуйными ставками.

Веб-интерфейс и аналитика: Развитие веб-панели для отображения накопленных данных. Планируется добавить больше интерактивных графиков и отчётов: например, страница матча с графиком изменения коэффициентов и пометками, где были аномалии; рейтинг букмекеров по числу аномалий; календарь “подозрительных” матчей и прочее. Такая аналитика даст возможность ретроспективно оценивать события и делать выводы (например, какие матчи впоследствии подтвердились как договорные).

Проект Oddly Odds Production стремится объединить в себе мощные инструменты для мониторинга линий ставок и сделать их доступными как профессиональным аналитикам, так и рядовым игрокам. Внедряя новые функции и улучшения, мы рассчитываем создать полноценный “радар аномалий” в беттинге, который в режиме реального времени отслеживает любые неординарные явления и помогает использовать эту информацию с пользой. Спасибо за внимание и успешного беттинга с Oddly Odds!

