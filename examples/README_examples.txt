📘 Примеры тестов Inforadar_Pro

1️⃣ sample_metrics.py — запускает тестовый Flask с метриками на порту 5050.
   Команда:
       python sample_metrics.py

2️⃣ cpu_load_generator.bat — создаёт искусственную нагрузку на процессор.
   Запусти его, и Prometheus через 1 минуту сгенерирует алерт “High CPU usage”.

3️⃣ test_alerts.yml — пример ручного алерта для проверки Alertmanager.
   Можно временно добавить в prometheus/rules для теста уведомлений.
